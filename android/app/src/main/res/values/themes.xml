<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartResourceAllocation" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="BottomNavIndicator">
        <item name="android:color">@android:color/white</item>
    </style>

    <style name="Theme.SmartResourceAllocation" parent="Base.Theme.SmartResourceAllocation" />
</resources>